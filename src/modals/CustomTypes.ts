export type Site={
    name: string,
    latitude: number,
    longitude: number,
    year: number,
    images: string[],
}

export type SiteRaw={
    name: string,
    latitude: string,
    longitude: string,
    year: string,
    images: string,

}